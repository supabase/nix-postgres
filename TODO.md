Austin's todo list, in no particular order:

- [ ] Upstream: Some work for missing extensions
- [ ] Upstream: create a `buildPostgresPackage` function
- [ ] Upstream: **cross version checks** to make sure our extensions are up to
      date with Supabase

<!-- split bullets -->
